/**
 * Метка сборки, видимая в интерфейсе.
 *
 * Нужна не для красоты: при разработке через Expo Go легко оказаться на старом
 * бандле — Metro кеширует, Expo Go держит прошлую загрузку, `git pull` мог не
 * пройти. Тогда починенные ошибки «не чинятся», и время уходит на диагностику
 * не того. Номер на экране отвечает на этот вопрос за одну секунду.
 *
 * Обновляется вручную при каждой отправке изменений.
 */
export const BUILD = 'сборка 9 · 28 июля';
