# BACKLOG

Всё, что сознательно отложено. Правило из [SPEC.md §20](./SPEC.md): если фича не описана в SPEC —
она не делается, а записывается сюда.

---

## Готово к работе (не требует физических устройств)

Это то, чем можно занять время, пока Фаза 0 упирается в устройства (см. ADR-007).
Порядок — по убыванию пользы.

- [~] **Спайк 4 — Stripe.** Код написан, арифметика и проверка подписи покрыты тестами.
      Остаток требует ключей Stripe и развёрнутого Supabase — чеклист в
      [`supabase/README.md`](../supabase/README.md). **Спайк не закрыт**, пока четыре тестовые
      карты §13.1 не прогнаны вживую.
- [x] **Схема БД + RLS.** `supabase/migrations/`, 26 проверок в `supabase/tests/rls_test.sql`.
      Условие §9 выполнено: прямая запись в `stakes`, `ledger_entries`, `vouchers`,
      `impact_ledger`, `subscriptions`, `alarm_runs` клиенту запрещена и политиками, и грантами.
- [x] **Тесты денежной логики** (§20.3): распределение сумм и округление центов — 18 тестов,
      проверка подписи вебхуков — 13 тестов.
- [ ] **Тесты стрика и поинтов** (§20.3, остаток): границы стрика при смене суток и часового
      пояса, расчёт поинтов с множителем ×1.5. Логика ещё не написана — по §20.3 тесты вперёд.
- [ ] **Ledger при успешном прохождении.** `planSuccessCharge` реализован, но эндпоинт
      `/alarm-runs/:id/complete` (§10), который его вызывает, — часть Фазы 2.
- [ ] **Проверка совместимости библиотек с New Architecture** (см. ADR-003): `react-native-vision-camera` v4,
      `react-native-sensors`, `react-native-reanimated` v3, `react-native-mmkv`.
- [ ] **Замена `ffmpeg-kit-react-native`.** Апстрим-проект архивирован и снят с публикации.
      Варианты: форк с самостоятельной сборкой бинарей, либо сразу нативный композитинг
      (`AVMutableVideoComposition` / `MediaCodec`+OpenGL) — SPEC §4.3 допускает оба пути.
      Влияет на срок Фазы 3 и на цель «экспорт ≤ 3 сек».
- [ ] **Договор с эко-фондом** (§6.15). До подписания формулировки в UI — только «направляем
      средства на посадку деревьев», с учётом обязательств. Гринвошинг = юридический риск.

## Замена моков демо на реальное (ADR-012)

Демо собрано на имитации данных. Порядок замены — по убыванию риска.

- [ ] **Нативный будильник** вместо кнопки «Промотать до утра» (§4.1). Пока не сделано,
      приложение никого не разбудит и не может показываться пользователям как будильник.
- [ ] **API-клиент** вместо `src/lib/demoServer.ts`. Экраны менять не придётся — граница
      проведена заранее.
- [ ] **Запись видео и композитинг 9:16** (§4.3) вместо заглушки кружка.
- [ ] **Онбординг с разрешениями и тестовым будильником** (§6.2) — по §4.1 обязателен: без него
      пользователь не узнает, что будильник у него не сработает.
- [ ] **Авторизация** (§6.3), **настройки** (§6.19), **выбор друзей** (§6.7).
- [ ] **Экран диагностики будильника** (§4.1) — статус всех разрешений.

## Требует физических устройств (блокирует Фазу 1)

- [ ] Спайк 1 — надёжный будильник Android. DoD: 20/20 при killed app на 3 вендорах.
- [ ] Спайк 2 — надёжный будильник iOS, подходы A + C.
- [ ] Матрица устройств §13.2.

## Человеческие действия, не кодовые

- [ ] **Спайк 2b — заявка в Apple на Critical Alerts entitlement.** Критический путь: рассмотрение
      занимает недели, по §17 подаётся в первую неделю разработки.
- [ ] Регистрация в Apple Small Business Program (§8.4) — комиссия 15% вместо 30%.
- [ ] ToS и Privacy Policy у юриста (§14.4, §19.5).
- [ ] Доступы: Apple Developer, Google Play Console, Stripe, Supabase (§19.3).
- [ ] Логотип и маскот в векторе (§19.4).

---

## Отложено в Фазу 2+ (из SPEC §2.2 — НЕ строить в MVP)

- Hold / преавторизация средств
- Многодневные челленджи (7/14/25 дней)
- Streak Freeze (платная заморозка стрика)
- Баннерная реклама
- Реальные партнёрские интеграции (в MVP только заглушки «скоро»)
- Розыгрыши / лотереи / билеты
- Глобальный лидерборд (в MVP только друзья)
- Внутренняя лента незнакомцев
- Чат в команде (в MVP только лента активности)
- Трекинг сна, анализ храпа
- Web-версия (см. ADR-004)
- Верификация прохождения испытания другом (§15) — в MVP не решаем
- Членство более чем в одной команде (§7.7 — в MVP ровно одна)
- Доступ к адресной книге (§6.7 — в MVP только инвайт-ссылки)

---

## Открытые вопросы к владельцу продукта

- **Deferred deep linking** (§7.7): инвайт-ссылка должна доводить нового пользователя до команды
  через установку приложения. Named в SPEC как «напр. Branch» — нужен выбор сервиса и бюджет.
- **Выбор аналитики** (§3.1): PostHog или Amplitude — решение не зафиксировано.
- **Push-инфраструктура** (§3.1): Supabase Edge Function или OneSignal — решение не зафиксировано.
- **RevenueCat** (§3.1) рекомендован, но не обязателен — подтвердить, т.к. влияет на Фазу 6.
